# 100 Days DSA Roadmap

### Plan

Practice DSA for 100 days (hopefully continuous). 

---

### Progress

Day 0 / Day 100

![Progress](https://img.shields.io/badge/Progress-0%20-blue) 

---

### All Problem Statements

<ol>
<li>Given a list of integers and a target value, find two distinct indices whose values sum up to the target. Return the pair of indices</li>
<li>Given daily stock prices, determine the maximum profit achievable by choosing one day to buy and a later day to sell</li>
<li>Given a list of integers, determine whether any value appears more than once</li>
<li>Given two strings, determine if one is an anagram of the other by comparing character frequencies</li>
<li>Group a list of strings into collections where each group contains words that are anagrams of each other</li>
<li>Given a list of integers, return a new list where each element is the product of all other elements except itself</li>
<li>Given a list of integers, find the contiguous subarray with the largest possible sum</li>
<li>Given an array and a window size, compute the maximum value in every sliding window position</li>
<li>Find the longest substring of a given string that contains no repeating characters</li>
<li>Given a string and a number k, find the length of the longest substring where you can replace at most k characters to make all characters identical</li>
<li>Given two strings, return the smallest substring in the first string that contains all characters of the second string</li>
<li>Move all zeroes in an array to the end while maintaining the relative order of non-zero elements</li>
<li>Return all unique triplets in a list of integers that sum up to zero</li>
<li>Given heights representing vertical lines, determine the maximum area of water that can be trapped between two lines</li>
<li>Rotate a square matrix 90 degrees clockwise in place</li>
<li>Reverse a singly linked list</li>
<li>Merge two sorted linked lists into a single sorted linked list</li>
<li>Detect whether a cycle exists in a linked list using two pointers</li>
<li>Reorder a linked list such that nodes are rearranged into first, last, second, second-last pattern</li>
<li>Remove the nth node from the end of a linked list</li>
<li>Add two numbers represented by linked lists where each node contains a single digit</li>
<li>Design a data structure that supports LRU (Least Recently Used) cache functionality</li>
<li>Determine if a string containing parentheses and brackets is valid based on proper closure</li>
<li>Implement a stack that supports retrieving the minimum value in constant time</li>
<li>Evaluate an expression written in Reverse Polish Notation</li>
<li>Given daily temperatures, determine for each day how many days you must wait until a warmer temperature occurs</li>
<li>Perform an inorder traversal of a binary tree</li>
<li>Perform a level order traversal of a binary tree</li>
<li>Determine the maximum depth of a binary tree</li>
<li>Determine whether a binary tree is height-balanced</li>
<li>Compute the diameter (longest path) of a binary tree</li>
<li>Given a binary tree and a target sum, determine if a root-to-leaf path exists that equals the sum</li>
<li>Reconstruct a binary tree using preorder and inorder traversal arrays</li>
<li>Find the lowest common ancestor of two nodes in a binary search tree</li>
<li>Implement a BST iterator supporting next() and hasNext()</li>
<li>Serialize and deserialize a binary tree to and from a string</li>
<li>Find the kth largest element in an array using a heap</li>
<li>Merge k sorted linked lists into a single sorted linked list</li>
<li>Implement a trie supporting insertion and prefix-based search</li>
<li>Find all words from a list that can be formed in a grid by adjacent letter traversal using backtracking and a trie</li>
<li>Implement binary search to locate a target in a sorted array</li>
<li>Search for a value in a rotated sorted array in logarithmic time</li>
<li>Find the minimum element in a rotated sorted array</li>
<li>Determine the minimum integer eating speed required for completing tasks within a limited number of hours</li>
<li>Given two sorted arrays, compute the median of the combined dataset in logarithmic time</li>
<li>Generate all possible subsets of a given list of numbers</li>
<li>Generate all unique subsets accounting for possible duplicates</li>
<li>Generate all permutations of a list of numbers</li>
<li>Find all combinations of numbers that sum to a target, with unlimited reuse allowed</li>
<li>Find combinations summing to a target but each element may be used only once</li>
<li>Solve the N-Queens puzzle and return all valid board configurations</li>
<li>Solve a partially filled Sudoku grid ensuring all rules are met</li>
<li>Given a phone number digit string, return all possible letter combinations</li>
<li>Given an array, determine if you can reach the last index following jump lengths</li>
<li>Compute the minimum number of jumps required to reach the end of an array</li>
<li>Determine if a complete circular gas station route can be completed</li>
<li>Insert a new interval into a list of existing intervals while maintaining sorted order</li>
<li>Merge overlapping intervals into non-overlapping intervals</li>
<li>Given intervals, determine the minimum number to remove to avoid overlapping</li>
<li>Given balloon intervals, find the minimum arrows required to burst all balloons</li>
<li>Implement merge sort on a list of numbers</li>
<li>Implement quicksort with partitioning logic</li>
<li>Find the kth smallest element using the quickselect algorithm</li>
<li>Implement counting sort for integers within a known limited range</li>
<li>Find the element that appears more than half the time in an array</li>
<li>Find the unique element where every other element appears twice</li>
<li>Count the number of 1 bits in an integer</li>
<li>Reverse the bit representation of a 32-bit number</li>
<li>Determine if an integer is a power of two</li>
<li>Add two integers without using + or - using bit operations</li>
<li>Compute how many distinct ways you can climb stairs if each step can be 1 or 2</li>
<li>Determine max money that can be robbed without alerting police (adjacent houses prohibited)</li>
<li>Same as House Robber but houses arranged in a circle</li>
<li>Given coin denominations, compute minimum coins needed to make a total</li>
<li>Count the number of ways to make a certain amount using given coin denominations</li>
<li>Determine if an array can be partitioned into two subsets with equal sum</li>
<li>Implement unbounded knapsack using DP</li>
<li>Implement 0/1 knapsack using DP</li>
<li>Find the longest increasing subsequence in an array</li>
<li>Compute the longest common subsequence between two strings</li>
<li>Compute the minimum edit distance between two strings</li>
<li>Decode a digit string following mapping rules</li>
<li>Determine if a string can be segmented into dictionary words</li>
<li>Count palindromic substrings in a string</li>
<li>Find the longest palindromic substring</li>
<li>Count unique paths from top-left to bottom-right of a grid</li>
<li>Count unique paths considering obstacles in grid</li>
<li>Compute the minimum path sum in a grid</li>
<li>Find the minimum path total in a triangle</li>
<li>Count the number of paths in a binary tree that sum to a target</li>
<li>Find the k most frequent elements in a list</li>
<li>Determine if it is possible to complete all courses given prerequisites</li>
<li>Return a valid order of courses given prerequisites</li>
<li>Count how many islands exist in a 2D grid</li>
<li>Clone an undirected graph</li>
<li>Determine time for all oranges to rot given spreading rules</li>
<li>Find shortest transformation steps from one word to another using a dictionary</li>
<li>Determine the ordering of characters in an alien language</li>
<li>Design a URL shortening system supporting encoding and decoding</li>
<li>Find shortest path in a binary matrix using BFS</li>
</ol>

--- 

### Notes:
Questions were ChatGPT generated, will change if not solvable as I move forward in this challenge.